簡易シューティング（C → JS移植）

- `syt_test.c`: 元のWindows向けC実装（修正済み：弾の当たり判定と境界チェックを堅牢化）
- `index.html` + `game.js`: ブラウザで動作するJS版（同等の挙動）

動かし方:
- `index.html` をブラウザで開くだけです。

操作:
- `A` / `D`: 左右移動
- `Space`: 発射
- `Q`: リセット（ブラウザ版ではゲームをリセットします）

追加要望があれば、当たり判定の微調整や描画改善を行います。